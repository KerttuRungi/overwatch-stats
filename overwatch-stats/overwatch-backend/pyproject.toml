[project]
name = "overwatch-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "KerttuRungi", email = "kerttu.rungi@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "alembic>=1.20.0",
    "fastapi[standard]>=0.141.1",
    "psycopg[binary]>=3.3.5",
    "sqlmodel>=0.0.42",
]

[tool.uv]
package = false
